import React from 'react';
import './HelpMessage.css'; // Optional: for styling

const HelpMessage = () => {
    return (
        <div className="help-message">
            Need help starting? Click Below
        </div>
    );
};

export default HelpMessage;